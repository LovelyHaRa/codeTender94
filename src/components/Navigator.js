import React from 'react'
import styled from 'styled-components'

const NavigatorBlock = styled.nav`
  position: fixed;
`

const Navigator = () => {
  return <NavigatorBlock></NavigatorBlock>
}

export default Navigator
